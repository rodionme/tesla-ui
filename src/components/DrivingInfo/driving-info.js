import React from 'react';
import './driving-info.css';

const DrivingInfo = ({ className }) => {
  return <div className={`driving-info ${className}`}>DrivingInfo</div>;
};

export default DrivingInfo;
