import React from 'react';
import './top-info-icons.css';

const TopInfoIcons = ({ className }) => {
  return <div className={`top-info-icons ${className}`}>TopInfoIcons</div>;
};

export default TopInfoIcons;
