import React from 'react';
import './speedometer.css';

const Speedometer = ({ className }) => {
  return <div className={`speedometer ${className}`}>Speedometer</div>;
};

export default Speedometer;
