import React from 'react';
import './action-icons.css';

const ActionIcons = ({ className }) => {
  return <div className={`action-icons ${className}`}>ActionIcons</div>;
};

export default ActionIcons;
