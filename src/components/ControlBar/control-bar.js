import React from 'react';
import './control-bar.css';

const ControlBar = ({ className }) => {
  return <section className={`control-bar ${className}`}>Control Bar</section>;
};

export default ControlBar;
